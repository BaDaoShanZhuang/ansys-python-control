# Mechanical scripts

Put Python snippets for `model.SendCommand(Language="Python", Command=...)` or PyMechanical `run_python_script_from_file(...)` here.

These scripts run inside Mechanical, so they can use `ExtAPI`, `DataModel`, `Model`, `Quantity`, and Mechanical object APIs.
